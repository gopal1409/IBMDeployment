apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginxapp-ingress-servie
  
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app1-nginx-service
            port:
              number: 80